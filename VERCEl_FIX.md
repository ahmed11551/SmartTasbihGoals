# 🔧 Исправление белого экрана на Vercel

## Проблема
Ошибка: `Uncaught SyntaxError: Unexpected token '<' (at main-39fdc1f380e122d9.js:1:1)`
Это означает, что браузер получает HTML вместо JavaScript файла.

## Причина
1. На Vercel статические файлы должны раздаваться автоматически из `outputDirectory`
2. Rewrites перехватывают запросы к статическим файлам
3. Неправильная конфигурация путей для Vite

## Решение

### 1. Исправить vercel.json
- Убрать упоминания `_next` (проект не Next.js)
- Правильно настроить rewrites для исключения статических файлов
- Статические файлы должны раздаваться напрямую

### 2. Убедиться, что:
- `outputDirectory: "dist/public"` правильный
- Статические файлы (JS, CSS) не попадают под rewrites
- Vite создает файлы в `/assets/` (проверить после сборки)

