# Анализ интеграции с Bot.e-replika.ru API

## Текущая интеграция

### ✅ Проксированные эндпоинты

1. **Habits** (`/api/habits`)
   - GET `/` - список привычек
   - GET `/:id` - одна привычка
   - POST `/` - создать привычку
   - PATCH `/:id` - обновить привычку
   - DELETE `/:id` - удалить привычку

2. **Tasks** (`/api/tasks`)
   - GET `/` - список задач
   - GET `/:id` - одна задача
   - POST `/` - создать задачу
   - PATCH `/:id` - обновить задачу
   - DELETE `/:id` - удалить задачу

3. **Goals** (`/api/goals`)
   - GET `/` - список целей
   - GET `/:id` - одна цель
   - POST `/` - создать цель
   - PATCH `/:id` - обновить цель
   - DELETE `/:id` - удалить цель

4. **Sessions** (`/api/sessions`)
   - GET `/` - список сессий
   - GET `/unfinished` - незавершенные сессии
   - GET `/:id` - одна сессия
   - POST `/` - создать сессию
   - PATCH `/:id` - обновить сессию

5. **Dhikr** (`/api/dhikr`)
   - GET `/catalog` - каталог дуа/азкаров
   - GET `/catalog/:category` - каталог по категории
   - GET `/logs` - логи зикров
   - GET `/logs/session/:sessionId` - логи сессии
   - POST `/logs` - создать лог
   - GET `/daily-azkar/:dateLocal` - ежедневные азкары
   - POST `/daily-azkar` - обновить ежедневные азкары

6. **Stats** (`/api/stats`)
   - GET `/` - статистика пользователя

7. **Badges** (`/api/badges`)
   - GET `/` - список бейджей
   - POST `/check` - проверить и присвоить бейджи
   - GET `/:id` - один бейдж

8. **Qaza** (`/api/qaza`)
   - GET `/` - данные о долге
   - POST `/calculate` - рассчитать долг
   - PATCH `/progress` - обновить прогресс
   - POST `/calendar/mark` - отметить день в календаре
   - GET `/calendar` - получить календарь
   - POST `/create-goal` - создать цель восполнения

9. **Category Streaks** (`/api/category-streaks`)
   - GET `/` - все streaks
   - POST `/update` - обновить streaks
   - GET `/:category` - streak категории

### ⚠️ Не проксированные эндпоинты (требуют интеграции)

1. **Auth** (`/api/auth`)
   - POST `/register` - регистрация (локальная БД)
   - POST `/login` - вход (локальная БД)
   - POST `/logout` - выход (сессия)
   - GET `/me` - текущий пользователь (локальная БД)
   - POST `/validate-token` - валидация токена (частично - только локальная проверка)

2. **Telegram** (`/api/telegram`)
   - POST `/auth` - авторизация через Telegram (локальная БД)

3. **AI** (`/api/ai`)
   - POST `/assistant` - AI ассистент (использует OpenAI напрямую, не Bot.e-replika.ru)

### 🔍 Потенциальные дополнительные эндпоинты от Bot.e-replika.ru

Согласно документации https://bot.e-replika.ru/docs, возможные дополнительные данные:

1. **Пользователи** (`/api/users` или `/api/profile`)
   - Профиль пользователя (расширенный)
   - Настройки пользователя
   - Подписка/тариф
   - История активности

2. **Уведомления** (`/api/notifications`)
   - Список уведомлений
   - Отметка как прочитано
   - Настройки уведомлений

3. **Контент** (помимо каталога дуа/азкаров)
   - Аудио файлы дуа
   - Переводы Корана
   - Хадисы
   - Статьи/материалы

4. **Социальные функции**
   - Друзья/подписчики
   - Общие цели/вызовы
   - Публичные достижения

5. **Аналитика и отчеты**
   - Детальная статистика за период
   - Экспорт данных
   - Графики прогресса

## Рекомендации

### Критичные для интеграции:

1. **Авторизация через Bot.e-replika.ru** (`/api/auth`)
   - Валидация токенов через API
   - Синхронизация пользователей
   - Получение профиля пользователя

2. **Telegram авторизация** (`/api/telegram/auth`)
   - Если Bot.e-replika.ru предоставляет единую систему аутентификации
   - Синхронизация Telegram ID с основной системой

### Опциональные:

3. **AI Assistant** (`/api/ai/assistant`)
   - Если Bot.e-replika.ru предоставляет AI функциональность
   - Можно переключиться с OpenAI на их API

4. **Уведомления** (`/api/notifications`)
   - Централизованная система уведомлений

5. **Контент** (аудио, переводы, материалы)
   - Для обогащения приложения

## Проблемы текущей реализации

1. **URL может быть неправильным**: 
   - Используется: `https://Bot.e-replika.ru/docs`
   - Возможно должно быть: `https://bot.e-replika.ru` или `https://bot.e-replika.ru/api`

2. **Авторизация не синхронизирована**:
   - Локальная регистрация/логин не синхронизируются с Bot.e-replika.ru
   - Токены валидируются только локально

3. **Дублирование данных**:
   - Данные могут храниться и в локальной БД, и в Bot.e-replika.ru
   - Нужна стратегия синхронизации

## Следующие шаги

1. Проверить правильный URL API (возможно через документацию или тестовые запросы)
2. Интегрировать `/api/auth` эндпоинты с Bot.e-replika.ru
3. Интегрировать `/api/telegram/auth` если доступно
4. Запросить документацию по дополнительным эндпоинтам
5. Реализовать синхронизацию данных между локальной БД и Bot.e-replika.ru

