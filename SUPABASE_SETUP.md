# 🚀 Настройка Supabase для Smart Tasbih Goals

## Шаг 1: Создать проект в Supabase

1. **Откройте**: https://supabase.com
2. **Нажмите "Start your project"** или **"Sign In"**
3. Войдите через GitHub (или создайте аккаунт)
4. **Нажмите "New Project"**
5. Заполните:
   - **Name**: `SmartTasbihGoals` (или любое другое название)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший к вам (например, `US East (North Virginia)`)
6. **Нажмите "Create new project"**
7. Подождите 1-2 минуты, пока проект создается

---

## Шаг 2: Получить Connection String

1. В Supabase Dashboard → ваш проект
2. Перейдите в **Settings** (шестеренка в левом меню)
3. Выберите **"Database"** в меню настроек
4. Прокрутите вниз до раздела **"Connection string"**
5. Выберите вкладку **"URI"** (не "Connection pooling")
6. Скопируйте connection string - он будет выглядеть так:

```
postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres
```

**⚠️ ВАЖНО:** Замените `[YOUR-PASSWORD]` на ваш пароль, который вы указали при создании проекта!

### Формат для Vercel:

```
postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxx.supabase.co:5432/postgres?sslmode=require
```

**Пример:**
```
postgresql://postgres:mySecurePassword123@db.abcdefghijklmnop.supabase.co:5432/postgres?sslmode=require
```

---

## Шаг 3: Применить миграции (создать таблицы)

### Способ A: Через Supabase SQL Editor (рекомендуется) ⭐

1. В Supabase Dashboard → ваш проект
2. Перейдите в **SQL Editor** (в левом меню)
3. Нажмите **"New query"**
4. Откройте файл `prisma/init-database.sql` из вашего проекта
5. **Скопируйте весь SQL код** (все 299 строк)
6. **Вставьте в SQL Editor**
7. **Нажмите "Run"** (или Ctrl+Enter)

Это создаст все таблицы, enum типы, индексы и связи.

### Способ B: Через Prisma CLI

```bash
# 1. Создайте .env.local файл с вашим connection string
echo 'DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxx.supabase.co:5432/postgres?sslmode=require"' > .env.local

# 2. Примените миграции
npx prisma migrate deploy
```

---

## Шаг 4: Добавить DATABASE_URL в Vercel

1. **Откройте**: https://vercel.com/dashboard
2. **Выберите проект** `SmartTasbihGoals`
3. Перейдите в **Settings** → **Environment Variables**
4. **Нажмите "Add New"** (или редактируйте существующий DATABASE_URL)
5. Заполните:
   ```
   Key: DATABASE_URL
   Value: postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxx.supabase.co:5432/postgres?sslmode=require
   Environment: Production, Preview, Development (выберите все три)
   ```
6. **Нажмите "Save"**

---

## Шаг 5: Добавить SESSION_SECRET (если еще не добавлен)

В том же разделе **Environment Variables**:

1. **Нажмите "Add New"**
2. Заполните:
   ```
   Key: SESSION_SECRET
   Value: [сгенерируйте случайную строку минимум 32 символа]
   ```

### Сгенерировать SESSION_SECRET:
```bash
openssl rand -base64 32
```

---

## Шаг 6: Передеплойть проект

1. В Vercel Dashboard → ваш проект
2. Перейдите в **Deployments**
3. Найдите последний деплой
4. Нажмите **"..."** (три точки) → **"Redeploy"**

---

## Шаг 7: Проверить работу

### Проверка 1: Логи Vercel
1. В Vercel Dashboard → ваш проект
2. Перейдите в **Functions** → **Logs**
3. **Должно быть**: `✅ Подключение к базе данных установлено`

### Проверка 2: Приложение
1. Откройте ваше приложение на Vercel
2. Откройте консоль браузера (F12)
3. **Не должно быть ошибок 500**
4. Попробуйте создать цель или привычку

---

## ✅ Готово!

После выполнения всех шагов:
- ✅ База данных Supabase настроена
- ✅ Все таблицы созданы
- ✅ Приложение подключено к БД
- ✅ API запросы работают

---

## 🆘 Troubleshooting

### Ошибка: "Password authentication failed"
- Проверьте, что пароль в connection string правильный
- Убедитесь, что заменили `[YOUR-PASSWORD]` на реальный пароль

### Ошибка: "Table does not exist"
- Миграции не применены
- Выполните Шаг 3 (применить миграции через SQL Editor)

### Ошибка: "Connection timeout"
- Проверьте, что используете правильный host (не localhost)
- Убедитесь, что добавлен `?sslmode=require` в connection string

---

## 📊 Преимущества Supabase

- ✅ Надежный PostgreSQL (базируется на AWS)
- ✅ Бесплатный план: 500MB базы данных
- ✅ Автоматические бэкапы
- ✅ Хорошая производительность
- ✅ Удобный SQL Editor
- ✅ Dashboard для управления данными

---

**После выполнения всех шагов приложение готово к использованию!** 🎉

