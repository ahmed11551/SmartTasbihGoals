# 🚀 Настройка DATABASE_URL в Vercel

## Шаг 1: Получить Connection String из Neon

1. **Откройте Neon Dashboard**: https://console.neon.tech
2. **Выберите ваш проект** (тот, который вы только что создали)
3. **На главной странице проекта** найдите раздел **"Connection Details"** или **"Connection string"**
4. **Нажмите на кнопку "Copy"** рядом с connection string
5. **Или перейдите в**: Dashboard → ваш проект → **"Connection Details"** → **"Connection string"**

### Формат connection string:
```
postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
```

**Важно:** Убедитесь, что connection string содержит `?sslmode=require` в конце!

---

## Шаг 2: Добавить DATABASE_URL в Vercel

1. **Откройте Vercel Dashboard**: https://vercel.com/dashboard
2. **Выберите ваш проект** `SmartTasbihGoals`
3. Перейдите в **Settings** → **Environment Variables**
4. **Нажмите "Add New"**
5. Заполните:
   - **Key:** `DATABASE_URL`
   - **Value:** вставьте connection string из Neon (весь текст, включая `postgresql://...`)
   - **Environment:** выберите **Production**, **Preview**, и **Development** (или только Production)
6. **Нажмите "Save"**

### Пример:
```
Key: DATABASE_URL
Value: postgresql://neondb_owner:your_password@ep-xxx-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
```

---

## Шаг 3: Добавить SESSION_SECRET (если еще не добавлен)

1. В том же разделе **Environment Variables**
2. **Нажмите "Add New"**
3. Заполните:
   - **Key:** `SESSION_SECRET`
   - **Value:** сгенерируйте случайную строку (минимум 32 символа)
   
### Как сгенерировать SESSION_SECRET:
```bash
# В терминале
openssl rand -base64 32
```

Или используйте онлайн генератор: https://randomkeygen.com/

---

## Шаг 4: Передеплойть проект

После добавления переменных окружения:

1. В Vercel Dashboard → ваш проект
2. Перейдите в **Deployments**
3. Найдите последний деплой
4. Нажмите **"..."** (три точки) → **"Redeploy"**
5. Или сделайте новый коммит в GitHub (автоматический деплой)

---

## Шаг 5: Применить миграции базы данных

После успешного деплоя нужно применить миграции Prisma:

### Вариант A: Через Vercel CLI (рекомендуется)

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Перейдите в папку проекта
cd /path/to/SmartTasbihGoals_pub-main

# Скачайте переменные окружения
vercel env pull .env.local

# Примените миграции
npx prisma migrate deploy
```

### Вариант B: Через Neon SQL Editor

1. Откройте Neon Dashboard → ваш проект
2. Перейдите в **SQL Editor**
3. Откройте файл `prisma/migrations/20250127000000_add_category_streaks/migration.sql`
4. Скопируйте SQL код из файла
5. Вставьте в SQL Editor и выполните

---

## Шаг 6: Проверить работу

1. **Откройте ваше приложение** на Vercel
2. **Откройте консоль браузера** (F12)
3. **Проверьте логи Vercel**: Dashboard → Functions → Logs
4. **Должно быть**: `✅ Подключение к базе данных установлено`

### Если видите ошибки:

- **503 ошибка** → DATABASE_URL не настроен или неправильный
- **P1001/P1002 ошибки** → База данных недоступна (проверьте Neon Dashboard)
- **Таблицы не найдены** → Миграции не применены (выполните шаг 5)

---

## ✅ Готово!

После выполнения всех шагов приложение должно работать корректно!

**Проверьте:**
- ✅ Все API запросы возвращают 200 (не 500)
- ✅ Можно создавать цели, привычки, задачи
- ✅ Тасбих счетчик работает
- ✅ Данные сохраняются в базе

---

## 🆘 Если что-то не работает

1. **Проверьте логи Vercel**: Dashboard → Functions → Logs
2. **Проверьте Neon Dashboard**: Убедитесь, что база данных активна
3. **Проверьте формат DATABASE_URL**: Должен начинаться с `postgresql://` и содержать `?sslmode=require`
4. **Проверьте переменные окружения**: Settings → Environment Variables

