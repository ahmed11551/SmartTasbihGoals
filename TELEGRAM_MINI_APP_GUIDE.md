# 🤖 Полная инструкция: Telegram Mini App

## 📋 Содержание

1. [Деплой на Vercel](#1-деплой-на-vercel)
2. [Создание Telegram бота](#2-создание-telegram-бота)
3. [Настройка Mini App](#3-настройка-mini-app)
4. [Интеграция с фронтендом](#4-интеграция-с-фронтендом)
5. [Настройка бэкенда](#5-настройка-бэкенда)
6. [Тестирование](#6-тестирование)

---

## 1. Деплой на Vercel

### Шаг 1.1: Подготовка

1. Убедитесь, что проект загружен в GitHub
2. Перейдите на https://vercel.com
3. Войдите через GitHub аккаунт

### Шаг 1.2: Импорт проекта

1. Нажмите **"Add New Project"**
2. Выберите репозиторий `SmartTasbihGoals`
3. Нажмите **"Import"**

### Шаг 1.3: Настройка проекта

**Framework Preset:** Other

**Root Directory:** `./`

**Build Command:**
```bash
npm run build
```

**Output Directory:**
```
dist/public
```

**Install Command:**
```bash
npm install
```

### Шаг 1.4: Переменные окружения

Добавьте в **Environment Variables**:

```
DATABASE_URL=your-postgresql-connection-string
SESSION_SECRET=your-secret-key-min-32-chars
TEST_TOKEN=test_token_123
BOT_REPLIKA_API_URL=https://Bot.e-replika.ru/docs
OPENAI_API_KEY=your-openai-key
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
NODE_ENV=production
PORT=3000
```

**Важно:** `TELEGRAM_BOT_TOKEN` получите после создания бота (см. ниже)

### Шаг 1.5: База данных

**Вариант A: Vercel Postgres (рекомендуется)**

1. Vercel Dashboard → **Storage** → **Create Database**
2. Выберите **Postgres**
3. Скопируйте `DATABASE_URL`
4. Добавьте в Environment Variables

**Вариант B: Внешняя БД**

Используйте Neon, Supabase или другую PostgreSQL БД.

### Шаг 1.6: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения (2-5 минут)
3. Скопируйте URL (например: `https://smart-tasbih-goals.vercel.app`)

### Шаг 1.7: Применить миграции

После деплоя создайте API endpoint для миграций или выполните вручную:

```bash
# Через Vercel CLI
vercel env pull .env.local
npx prisma migrate deploy
```

---

## 2. Создание Telegram бота

### Шаг 2.1: Найти BotFather

1. Откройте Telegram
2. Найдите `@BotFather` в поиске
3. Нажмите **"Start"**

### Шаг 2.2: Создать бота

Отправьте команду:
```
/newbot
```

**Шаги:**
1. **Имя бота:** `Умный Тасбих` (или любое другое)
2. **Username бота:** `SmartTasbihBot` (должен заканчиваться на `bot`)

**Результат:**
BotFather выдаст токен вида:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**⚠️ Сохраните этот токен!** Он понадобится для:
- Настройки Mini App
- Переменной окружения `TELEGRAM_BOT_TOKEN`

### Шаг 2.3: Настроить описание

```
/setdescription
```

Выберите бота и введите:
```
Приложение для отслеживания духовных практик: тасбих, зикры, цели и привычки
```

### Шаг 2.4: Настроить команды

```
/setcommands
```

Выберите бота и введите:
```
start - Начать работу с ботом
help - Помощь и инструкции
app - Открыть приложение
```

### Шаг 2.5: Настроить аватар (опционально)

```
/setuserpic
```

Выберите бота и отправьте изображение.

---

## 3. Настройка Mini App

### Шаг 3.1: Установить кнопку меню

Отправьте BotFather:
```
/setmenubutton
```

**Шаги:**
1. Выберите вашего бота
2. Введите текст кнопки: `Открыть приложение`
3. Введите URL вашего Vercel приложения:
   ```
   https://your-app.vercel.app
   ```

### Шаг 3.2: Альтернатива - Web App

Можно также использовать команду:
```
/setwebapp
```

Выберите бота и введите URL:
```
https://your-app.vercel.app
```

---

## 4. Интеграция с фронтендом

### ✅ Уже реализовано:

1. ✅ `client/src/lib/telegram.ts` - утилиты для Telegram WebApp
2. ✅ `client/src/components/TelegramAuth.tsx` - авторизация через Telegram
3. ✅ `client/index.html` - добавлен Telegram WebApp SDK
4. ✅ `client/src/main.tsx` - инициализация Telegram WebApp
5. ✅ `client/src/App.tsx` - подключен TelegramAuth компонент

### Использование в компонентах:

```typescript
import { 
  isTelegramWebApp, 
  getTelegramUser,
  hapticFeedback,
  showMainButton,
  hideMainButton 
} from '@/lib/telegram';

// Проверить что в Telegram
if (isTelegramWebApp()) {
  // Получить пользователя
  const user = getTelegramUser();
  console.log('Telegram user:', user);
  
  // Вибрация
  hapticFeedback('impact', 'medium');
  
  // Показать кнопку
  showMainButton('Сохранить', () => {
    // Действие
  });
}
```

---

## 5. Настройка бэкенда

### ✅ Уже реализовано:

1. ✅ `server/routes/telegram.ts` - роут для Telegram авторизации
2. ✅ `server/routes.ts` - подключен telegram роут
3. ✅ `server/index.ts` - добавлен CORS для Telegram

### Переменные окружения:

Добавьте в Vercel:
```
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
```

### Валидация данных:

Бэкенд автоматически валидирует данные от Telegram используя HMAC SHA256.

---

## 6. Тестирование

### Шаг 6.1: Локальное тестирование

1. Запустите приложение:
   ```bash
   npm run dev
   ```

2. Используйте ngrok для туннеля:
   ```bash
   ngrok http 5000
   ```

3. Скопируйте ngrok URL (например: `https://abc123.ngrok.io`)

4. В BotFather:
   ```
   /setmenubutton
   ```
   Введите ngrok URL

5. Откройте бота в Telegram и нажмите кнопку меню

### Шаг 6.2: Продакшн тестирование

1. Убедитесь, что проект задеплоен на Vercel
2. В BotFather обновите URL на Vercel URL
3. Откройте бота в Telegram
4. Нажмите кнопку меню
5. Приложение должно открыться в Telegram

---

## 📱 Дополнительные возможности

### Haptic Feedback (вибрация)

```typescript
import { hapticFeedback } from '@/lib/telegram';

// Легкая вибрация
hapticFeedback('impact', 'light');

// Средняя вибрация
hapticFeedback('impact', 'medium');

// Сильная вибрация
hapticFeedback('impact', 'heavy');
```

### Главная кнопка

```typescript
import { showMainButton, hideMainButton } from '@/lib/telegram';

// Показать кнопку
showMainButton('Сохранить', () => {
  // Сохранить данные
  saveData();
  hideMainButton();
});
```

### Back кнопка

```typescript
import { showBackButton, hideBackButton } from '@/lib/telegram';

// Показать кнопку "Назад"
showBackButton(() => {
  navigate('/');
  hideBackButton();
});
```

### Уведомления

```typescript
import { showTelegramAlert, showTelegramConfirm } from '@/lib/telegram';

// Показать alert
showTelegramAlert('Данные сохранены!');

// Показать confirm
const confirmed = await showTelegramConfirm('Удалить задачу?');
if (confirmed) {
  deleteTask();
}
```

### Закрытие приложения

```typescript
import { closeTelegramWebApp } from '@/lib/telegram';

// Закрыть Mini App
closeTelegramWebApp();
```

---

## 🔒 Безопасность

### Валидация данных Telegram

Бэкенд автоматически валидирует `initData` от Telegram используя HMAC SHA256. Это гарантирует, что данные действительно пришли от Telegram.

### Переменные окружения

**Никогда не коммитьте:**
- `TELEGRAM_BOT_TOKEN`
- `SESSION_SECRET`
- `DATABASE_URL`
- `OPENAI_API_KEY`

Все эти переменные должны быть только в Vercel Environment Variables.

---

## 📊 Структура проекта для Telegram

```
├── client/
│   ├── src/
│   │   ├── lib/
│   │   │   └── telegram.ts          # Telegram WebApp утилиты
│   │   ├── components/
│   │   │   └── TelegramAuth.tsx     # Авторизация через Telegram
│   │   └── App.tsx                   # Подключен TelegramAuth
│   └── index.html                    # Telegram WebApp SDK
├── server/
│   ├── routes/
│   │   └── telegram.ts               # Telegram auth endpoint
│   └── index.ts                      # CORS для Telegram
└── vercel.json                       # Конфигурация Vercel
```

---

## ✅ Чеклист готовности

- [ ] Проект задеплоен на Vercel
- [ ] Получен URL приложения
- [ ] Создан бот через BotFather
- [ ] Получен токен бота
- [ ] Токен добавлен в Vercel Environment Variables
- [ ] Настроена кнопка меню в BotFather
- [ ] URL приложения указан в BotFather
- [ ] База данных настроена и миграции применены
- [ ] Протестировано локально (через ngrok)
- [ ] Протестировано в продакшене

---

## 🎉 Готово!

После выполнения всех шагов ваш Telegram Mini App будет доступен:

1. Откройте бота в Telegram
2. Нажмите кнопку меню (или команду `/app`)
3. Приложение откроется внутри Telegram

**Репозиторий:** https://github.com/ahmed11551/SmartTasbihGoals

